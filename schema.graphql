type HolderBao @entity {
  id: ID!
  address: Bytes!
  balance: BigInt!
}

type HolderBaocx @entity {
  id: ID!
  address: Bytes!
  balance: BigInt!
}

type LockedBaocx @entity {
  id: ID!
  address: Bytes!
  lock: BigInt!
}

type HoldertBao @entity {
  id: ID!
  address: Bytes!
  balance: BigInt!
}